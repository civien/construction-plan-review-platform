# 施工方案审查平台

多类型专项施工方案符合性审查 Web 平台：支持深基坑等多种类型，规则可在线增改，审查判读接入国产大模型（默认 DeepSeek，可切换通义千问/智谱GLM/Kimi）。

## 目录结构
```
施工方案审查平台/
├── backend/                # FastAPI 后端 + 审查引擎
│   ├── app.py             # API 路由（审查/规则/设置/历史/示例）
│   ├── engine.py          # 抽取 docx → 机械分析 → 生成批注 docx
│   ├── llm.py             # OpenAI 兼容大模型客户端 + Prompt
│   ├── rules_store.py     # 规则仓库（rules/<type>.json）
│   ├── db.py              # SQLite（历史 + 设置）
│   ├── rules/excavation.json   # 深基坑规则（示例）
│   └── data/              # 上传/历史/示例数据（含 example 演示稿）
└── frontend/              # React + Vite 前端（三页）
    └── src/App.jsx        # 审查工作台 / 规则管理 / 模型设置
```

## 启动
```bash
# 方式一：一键启动（后端同源托管已构建的前端）
bash run.sh
# 浏览器打开 http://127.0.0.1:8000

# 方式二：前端热开发（改完前端用这个，自动代理 /api 到 8000）
cd backend && <python> -m uvicorn app:app --port 8000   # 先起后端
cd frontend && npm install && npm run dev               # 再起前端 http://127.0.0.1:5173
```
> 若改动过 frontend，记得 `cd frontend && npm run build` 重新生成 dist，方式一才会生效。

## 使用流程
1. **模型设置**：填供应商、API Key、模型名 → 「测试连接」→「保存」（未配置时审查会提示先用示例演示）。
2. **审查工作台**：选方案类型（如深基坑）→ 上传 .docx → 「开始审查」→ 查看按严重度分组的意见 → 下载带 Word 批注的退回稿 / Markdown 报告。
3. **规则管理**：点类型卡片在线编辑严重缺陷 / 规范版本表 / 清单 / 跨章节检查项 / 目录模板；「新增类型」可建模板、脚手架、起重等新类型；保存即时生效。
4. **示例演示**：未配大模型也可点「示例演示」查看内置深基坑审查结果与批注稿效果。

## 新增一种方案类型
在【规则管理】点「新增类型」，填标识（如 `scaffold`）与名称，系统生成模板；按该类型规范改写严重缺陷清单、规范版本表、核查清单、跨章节关键词与目录模板即可。引擎与类型无关，无需改代码。

## 说明
- 机械检查（结构完整性 / 规范版本 / 跨章节一致性）由引擎本地完成；最终判定与整改建议由大模型结合全文给出。
- 批注稿为方案 docx 副本，每条意见锚定到对应段落，可直接退回编制单位修改。
- 历史记录在【审查工作台】底部，可回看并重新下载批注稿与报告。
