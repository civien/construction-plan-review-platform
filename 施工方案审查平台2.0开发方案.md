# 施工方案审查平台 2.0 开发方案

> 编制依据：`/Users/civien/WorkBuddy/深基坑施工方案/施工方案评审规则/` 全部 34 个规则文件
> 现状基线：1.0 平台（`good-20260726` 标签）含 4 类方案（excavation / crane-lifting / formwork-support / scaffold），规则来源为旧 SKILL.md
> 目标：把 2.0 重新制定的 34 套新规则（10 大类）接入平台，并增强审查引擎

---

## 一、规则资料全面分析

### 1.1 资料清单：10 大类 / 34 个细分规则

| 大类文件夹 | 子类型文件数 | 文件名（节选） |
|---|---|---|
| 基坑工程专项施工方案的详细评审要点 | 6 | SMW工法桩 / 放坡 / 锚杆支护 / 土钉墙支护 / 钢板桩支护 / 灌注桩排桩支护 |
| 脚手架工程方案审查要点 | 8 | 悬挑扣件式 / 悬挑盘扣式 / 落地扣件式 / 落地盘扣式 / 吊篮 / 卸料平台(悬挑·落地扣件·落地盘扣) |
| 模板及支撑体系工程审查要点 | 3 | 高支模扣件式 / 翻模(SKILL) / 高支模盘扣式(SKILL) |
| 起重吊装及安装拆卸工程审查要点 | 6 | 履带吊 / 龙门吊 / 塔式起重机 / 施工升降机 / 构件设备吊装单机(SKILL) / 构件设备吊装多机抬吊 |
| 风电工程施工方案 | 4 | 海上风机吊装 / 陆上混塔吊装 / 陆上钢塔吊装 / 塔筒风机运输 |
| 光伏工程施工方案 | 1 | 山地光伏安装(含运输) |
| 人工挖孔桩工程施工方案 | 1 | 人工挖孔桩 |
| 暗挖工程施工方案审查要点 | 2 | 盾构 / 顶管沉井 |
| 临时用电工程施工方案审查要点 | 1 | 临时用电 |
| 钢结构安装工程专项施工方案评审要点 | 2 | 钢结构安装 / 网架安装 |

> 1.0 已有的 4 类（深基坑/起重/模板支撑/脚手架）在 2.0 中**细化并扩展**了：深基坑→基坑工程 6 子类、脚手架 8 子类、模板 3 子类、起重 6 子类；**全新增加 6 大类**（光伏/风电/人工挖孔桩/暗挖/临时用电/钢结构）。

### 1.2 统一的「12 节标准模板」（34 个文件 100% 一致）

每个规则文件结构完全相同（统计实证：H1×1、H2×2、H3×12，关键词命中高度一致）：

- **第 1 章 专用严重缺陷清单**（表格：序号 / 分类 / 严重缺陷情形）
  - 1.1 定位指南（表格：缺陷情形 / 主要检查章节 / 定位方法·关键词搜索）★比 1.0 多了"关键词/定位方法"，机器可定位
- **第 2 章 详细核查清单及评审要点**（11 个子节）
  - 2.1 整体评审（表格：检查项 / 评审要点）
    - **2.1.1 快速矛盾检测（grep 交叉比对）** ★2.0 新增强能力：检测项 + 搜索关键词 + 检查要点
  - 2.2 工程概况（参考要点 + 「【编号.细分】是否…」核查 bullet）
  - 2.3 编制依据（含 **2.3.1 规范版本有效性核查表**：规范名 / 最新版本 / 旧版本 / 备注）
  - 2.4 施工计划
  - 2.5 施工工艺技术
  - 2.6 施工保证措施
  - 2.7 施工管理及作业人员配备和分工
  - 2.8 验收要求
  - 2.9 应急处置措施
  - 2.10 计算书及相关图纸
  - **2.11 跨章节一致性检查**（表格：一致性检查项 / 关联章节 / 常见问题示例）

> 文件头还有「适用范围」「覆盖章节」说明性文本；SKILL.md 额外带 `name:` / `## description:` frontmatter。

### 1.3 与 1.0 平台规则 schema 的映射关系

| 2.0 规则位置 | 1.0 平台已有字段 | 差异 |
|---|---|---|
| 第1章 + 1.1 | `severe_defects[{no,category,desc,location,skill}]` | 2.0 多了"关键词/定位方法"，定位更精确 |
| 2.3.1 规范版本表 | `norm_versions{name/note/wrong}` | 字段完全对应，2.0 还含"最新版本号/旧版本号" |
| 2.2–2.10 核查 bullet | `checklist[{chapter,subtag,req}]` | 完全对应（chapter=子节名，subtag=【编号.细分】，req=是否…） |
| 2.11 跨章节表 | `cross_chapter_checks[{key,keywords,chapters,problem}]` | 完全对应 |
| **2.1.1 快速矛盾检测** | ❌ 无 | **2.0 新增**，可做成零成本机器检查 |

### 1.4 2.0 相比 1.0 的关键升级点

1. **规则粒度**：4 类 → 10 大类 / 34 子类型（二级分类）。
2. **规则深度**：每个子类型独立成册，参数级核查（如 SMW 工法桩的水泥掺量/水灰比/桩长/型钢规格）。
3. **机器可定位**：严重缺陷带"关键词搜索"，2.1.1 带"快速矛盾检测"的 grep 关键词 → 引擎可在 LLM 之前做确定性检查。
4. **规范版本核查**：2.3.1 明确给出"最新版本 vs 已废止版本"，可自动识别方案引用过期规范。
5. **跨章节一致性**：2.11 用关联章节+常见矛盾示例，可直接驱动自动比对。

### 1.5 两种格式差异（解析需兼容）

- **普通 `.md`**（31 个）：`# 标题` + `**适用范围**:` / `**覆盖章节**:` 开头。
- **`-SKILL.md`**（3 个：翻模 / 高支模盘扣式 / 构件设备吊装单机）：顶部多了 `name: "xxx"` 与 `## description: "..."` 的 YAML 风格 frontmatter，正文结构与普通 md 完全一致。
- 解析器必须同时支持两种，从 `name:`（有则用，否则由文件名生成）取规则 ID，从 `description:`（有则用）取说明。

---

## 二、2.0 平台架构升级方案

### 2.1 规则数据层（schema 扩展）

在现有 `rules/*.json` 字段基础上扩展（`backend/rules/<type>.json`）：

```jsonc
{
  "name": "climbing-formwork-review",     // SKILL.md 的 name；普通 md 由文件名生成
  "display_name": "翻模工程专项施工方案",
  "category": "模板及支撑体系工程",        // 新增：大类，用于二级树
  "applicable_scope": "翻模（液压爬升翻模）工程…", // 新增：文件头适用范围
  "cover_chapters": "工程概况、编制依据、…",        // 新增：覆盖章节
  "description": "用于审核翻模、爬模…",             // SKILL.md description（可选）
  "severe_defects": [
    { "no":1, "category":"翻模工程", "desc":"…", "location":"第4.2节", "keywords":["附着支撑","承载体"] }
  ],
  "norm_versions": { "JGJ 120": {"latest":"2012","wrong":"—","note":"基坑支护核心规程"} },
  "checklist": [
    { "chapter":"2.2 工程概况", "subtag":"1.工程概况", "req":"是否包含工程基本情况…" }
  ],
  "quick_conflict_checks": [               // 新增：2.1.1 快速矛盾检测
    { "item":"基坑深度与SMW桩长一致性", "keywords":["基坑深度","桩长","嵌固深度"], "check":"工程概况/计算书/图纸中描述应一致" }
  ],
  "cross_chapter_checks": [
    { "key":"基坑深度与SMW桩长", "chapters":"概况↔技术参数↔计算书↔图纸", "problem":"概况深12m，计算书嵌固6m…" }
  ]
}
```

### 2.2 解析自动化（md → rule）

新增 `backend/tools/md_to_rule.py`：
- 输入：一个 `.md` 规则文件；输出：对应 `rules/<type>.json`
- 解析步骤：标题树定位各章 → 正则提取表格（严重缺陷/规范版本/跨章节）→ 提取 bullet（核查清单）→ 提取 2.1.1 的"检测项+关键词+检查要点" → 兼容 SKILL.md frontmatter
- 批量：遍历 34 文件生成 34 个 JSON（或按 10 大类归并）；`rules_store.py` 改为按 `category` 分组返回二级树

### 2.3 审查引擎增强（零成本机器检查）

现有三阶段：`extract(docx→text)` → `mechanical(grep)` → `LLM(语义判读)` → `批注`。
- 在 **mechanical 阶段新增 quick_conflict_checks 执行**：用 `keywords` 在 `full_text` 中 grep，按 `check` 提示比对，输出矛盾项（红/黄），**不消耗 LLM token、确定性高**
- LLM 提示词（`llm.py` 的 `build_review_messages`）补充 `quick_conflict_checks` 字段与规范版本核查指令
- 批注输出新增「快速矛盾检测」区块

### 2.4 前端 UI（二级类型选择）

- `/api/types` 改为树：`[{category, sub_types:[{type,name,rule_file}]}]`
- 审查页方案类型下拉改为「大类 → 子类型」两级联动
- 规则管理左侧改为大类树，点击子类型加载对应规则，支持新增/编辑（兼容新字段）
- 审查报告新增「快速矛盾检测」结果分区

### 2.5 兼容与迁移（git 分支保护 1.0）

- 从 `good-20260726` 切出开发分支 `v2.0`，**不破坏已备份的好版本**
- 旧 4 类 rules 保留，2.0 的 34 子类型叠加；前端二级树中 1.0 四类可归入对应大类或单独保留
- 回退方式不变：`git checkout good-20260726`

---

## 三、实施计划（建议分阶段）

| 阶段 | 任务 | 产出 | 验证 |
|---|---|---|---|
| P0 | 写 `md_to_rule.py` 解析器 + 字段 schema | 解析脚本 | 先跑翻模(SKILL)+SMW(普通)2 个样本，人工核对 JSON 字段完整 |
| P1 | 批量解析 34 文件 → 34 个 rules JSON | `rules/*.json` 扩充 | 校验 JSON 合法、category 归类正确 |
| P2 | 引擎接入 `quick_conflict_checks`（机械阶段） | llm.py / engine 增强 | 用一个真实方案跑，看矛盾检测输出 |
| P3 | 前端二级类型 + 规则管理树 + 报告分区 | 前端改造 | 选子类型→上传→审查全流程 |
| P4 | 联调 + 回归 1.0 四类 + 部署 | 全量验证 | 旧 4 类 + 新 34 子类型均通过 |

> 建议先做 P0（解析器 POC，验证 2 个样本），确认字段映射无误后再批量铺开，降低返工。

---

## 四、风险与建议

1. **篇幅大**：34 文件约 900KB 纯文本，批量解析后 rules 目录会明显增大，但均为静态 JSON，不影响运行性能。
2. **解析鲁棒性**：表格/ bullet 写法偶有细微差异（如 `#### 2.1.1` 用 `检测项1：` 或 `**检测项1**` 不同风格），解析器需做容错（以"序号/关键词/检查要点"语义列提取，而非死板按行列号）。
3. **二级类型 UX**：34 子类型对用户选择有门槛，建议：①默认按大类展示、搜索框快速定位；②上传方案时支持"自动识别类型"（用文件名/标题关键词匹配最相近子类型）。
4. **规范版本时效**：2.3.1 表中的"最新版本"需定期复核（如 JGJ 120 若有新版），建议规则管理里规范版本支持单独维护。
5. **建议 POC 先行**：先解析 2 个代表样本（1 个 SKILL.md + 1 个普通 md）验证 schema，再批量，避免 34 个一起返工。

---

## 五、下一步建议（已确认方向）

> 用户已于 2026-07-31 确认：① 粒度采用「10 大类 / 34 子类型」二级结构；② 旧 4 类（1.0）仅作历史归档、不进 2.0；③ 先做 P0 解析器 POC（翻模 SKILL + SMW 普通 2 个样本，已交付并核对字段准确）。

## 六、UI/UX 设计规范（2.0 原型要求，用户评审后补充）

### 6.1 总体设计语言（专业、克制、信息清晰）
- **配色**：主色专业蓝 `#2563eb`（深蓝 `#1e3a8a` 用于标题/强调）；中性灰阶（背景 `#f5f7fa`、卡片 `#fff`、边框 `#e5e7eb`、文字 `#1f2937`/`#6b7280`）；语义色：严重缺陷/错误红 `#dc2626`、矛盾/警告橙 `#ea580c`、通过绿 `#16a34a`、提示黄 `#ca8a04`。
- **布局**：顶部品牌栏（平台名 + 全局操作）；左侧固定主导航（审查工作台 / 规则管理 / 模型设置 / 审查历史）；内容区卡片化、充足留白、细边框、克制阴影、清晰层级。
- **排版**：中文无衬线（PingFang SC / Microsoft YaHei / system-ui）；正文 14px、标题 16–20px；表格/清单紧凑但有行间距。

### 6.2 审查工作台（一级 / 二级方案类型树 + 自动匹配规则）
- 左侧「一级大类 → 二级子类型」可折叠树（10 大类 / 34 子类型）。点击二级子类型 → 右侧主区显示「已匹配规则：<规则名>」并高亮，上传/审查按钮启用；未选时按钮禁用并提示。
- 中部：方案文件上传区（拖拽 + 选择 .docx）、审查设置（模型供应商/模型下拉，预填默认 deepseek-v4-flash）。
- 右侧/下方：审查结果区，含标准分区：①严重缺陷核查 ②详细核查清单 ③快速矛盾检测（2.0 新增，红/橙高亮矛盾项）④跨章节一致性 ⑤规范版本核查。各分区可折叠。

### 6.3 规则管理（一级 / 二级树 + 长内容折叠 Accordion）
- 左侧同上「大类 → 子类型」树，点击子类型加载其规则详情。
- 右侧规则详情用 **可折叠分区（Accordion）**：严重缺陷清单 / 规范版本有效性 / 详细核查清单（2.2–2.10）/ 跨章节一致性（2.11）/ 快速矛盾检测（2.1.1）各自一个折叠块；长清单默认收起、展开可滚动；支持单条编辑、新增、删除（沿用 1.0 CRUD）。
- 顶部提供搜索框（按规则名/关键词快速定位子类型）。

### 6.4 模型设置
- 供应商卡片选择（DeepSeek / 通义 / 智谱 / Kimi），选中高亮；模型下拉（随供应商变化，datalist 提示预置模型）；API Key 输入（密码框）；「测试连接」按钮 + 状态反馈。

### 6.5 原型交付与评审闭环
- 先做**单文件 HTML 高保真原型**（vanilla HTML/CSS/JS，离线可开），覆盖审查工作台 / 规则管理 / 模型设置三页，体现上述二级树、自动匹配、折叠交互；用户评审风格后再写 React 正式前端，避免返工。
